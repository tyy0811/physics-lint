# V1.1 backlog

Items deferred from V1 with explicit forward-references in V1 documentation.
Each entry must cite the V1 source (`docs/tradeoffs.md`, plan section, or rule
docstring) that points at it. Entries are appended as they are deferred;
they are not retroactively edited.

---

## 2026-04-15: pointwise `MeshField.laplacian()` via SPR-recovered double differentiation

**V1 source:** `docs/tradeoffs.md` 2026-04-15 entry; `src/physics_lint/field/mesh.py` module docstring; `docs/plans/2026-04-28-physics-lint-v1-week-3.md` Task 5.

**V1 state:** `MeshField.laplacian()` raises `NotImplementedError`; users are redirected to `MeshField.laplacian_l2_projected_zero_trace()` (interior O(h²) on smooth fields, boundary DOFs hard-pinned to zero by the Dirichlet-condensed L² projection onto `V_{h,0}`).

**V1.1 target:** implement a true pointwise Laplacian on `MeshField` satisfying the `Field.laplacian()` ABC contract via Superconvergent Patch Recovery (SPR) on the gradient field, then differentiated again. References: Zienkiewicz & Zhu 1992 ("The superconvergent patch recovery and a posteriori error estimates"), Babuška & Strouboulis 2001 §3.

**Acceptance criteria:**

- `MeshField.laplacian()` returns a `Field` whose `values()` approximates pointwise `Δu` to O(h²) on smooth analytical solutions (current PH-CON-004 `sin(πx)sin(πy)` and `x(1-x)y(1-y)` test cases).
- Boundary DOFs return non-zero approximations of true boundary `Δu`: specifically, on `x(1-x)y(1-y)` the boundary corner value approximates ≈ −0.5, not 0.
- PH-CON-004's boundary-element exclusion (`tests/rules/test_ph_con_004.py` and `src/physics_lint/rules/ph_con_004.py`) can be removed without changing test outcomes — i.e. boundary elements stop reflecting an operator artifact and start reflecting true conservation behavior.
- A new test file `tests/test_meshfield_laplacian_pointwise.py` validates both criteria independently of PH-CON-004.

**V1.1 implementation cost estimate:** ~1-2 days. Not in V1 because (a) requires its own design pass on the SPR algorithm choice (patch topology, weighting, gradient recovery technique), (b) blocks no V1 functionality — the V1 zero-trace operator is adequate for PH-CON-004's interior-only consumption, (c) Week 3's `MeshField` surface area was already at the spike-validated boundary and expanding it further would have broken the week's schedule hedge.

**Related V1 design discipline:**

- Adding a new `MeshField` operator in V1.1 must follow the "cite the target operation, not the matrices" discipline from `docs/tradeoffs.md`. The new `.laplacian()` is a pointwise pointwise-approximation operator; the existing `.laplacian_l2_projected_zero_trace()` is an L² projection onto `V_{h,0}`. These two operations share function-space ingredients but compute different objects and must retain distinct names.
- The Week-4 Codex review prompt (Task 8 Step 0) includes a formula-vs-plan audit as item (k); the V1.1 pointwise Laplacian work will re-trigger that audit on the new code path.

---

## 2026-04-16: restore 6-surrogate dogfood — expand Week 2½ 3-axis comparison to ensemble, OT-CFM, DPS (+ improved DDPM, flow-matching)

**V1 source:** `docs/tradeoffs.md` 2026-04-16 entries (Fallback D', Criterion 3); `dogfood/run_dogfood_real.py`; `docs/superpowers/specs/2026-04-17-week-2.5-dogfood-a1-design.md`.

**V1 state.** Week 2½ A1 real-ML run covers 3 of the 6 originally-planned surrogates (`unet_regressor`, `fno`, `ddpm`) from local `laplace-uq-bench/experiments/` checkpoints, through a 3-axis cross-comparison (PH-RES-001 vs `pde_residual` L²-vs-L² sanity, PH-BC-001 vs `bc_err`, PH-POS-002 vs `max_viol`) with a 4-way verdict (`PASS (scoped)` / `PASS (scoped, MIXED)` / `FAIL` / `BUG`). This replaces the original top-2/bottom-2 H¹-vs-H⁻¹ ranking check because laplace-uq-bench publishes no H¹ column and PH-RES-001 falls back to L² on non-periodic Laplace (see `docs/tradeoffs.md` Criterion 3 entry for the reinterpretation). Three surrogates are deferred:
- **Ensemble** (local `experiments/ensemble_phase2/best.pt` exists): excluded from A1 because laplace-uq-bench's published results table has no `pde_residual` column for the ensemble, so the L²-vs-L² sanity axis is uncheckable. Requires either upstream adding the ensemble row or reinterpreting A1's verdict rule for partial-axis surrogates.
- **OT-CFM**: no local checkpoint; the `configs/flow_matching.yaml` training run was never executed or its weights were not retained. (Upstream names this "flow matching"; the plan's original "OT-CFM" label refers to the same model family.)
- **DPS**: no new weights needed — DPS is a conditional sampler over the unconditional DDPM, implemented at `src/diffphys/model/dps_sampler.py`. Deferred because wiring it into the dogfood extraction path requires a separate subprocess target and upstream metric computation path that A1 did not budget.

Upstream has also introduced two surrogates that post-date the original plan: **improved DDPM** (`configs/ddpm_improved.yaml`) and **flow matching** (`configs/flow_matching.yaml`). V1.1's target is to cover these as well, bringing the expanded scope to **≥ 6 surrogates** (original 6 minus DPS-overlap-with-DDPM plus improved DDPM and flow matching).

**V1.1 target.** Expand the Week 2½ A1 harness to cover ≥ 6 surrogates under the same 3-axis cross-comparison framework: `unet_regressor`, `fno`, `ddpm`, `ensemble_phase2`, `ddpm_improved`, `flow_matching`, plus DPS as a DDPM-conditional path. Revisit whether an H¹-comparable ranking metric can be reintroduced (would require upstream adding H¹ to its results table, or physics-lint implementing its own periodic-approximation scheme); if not, ship v1.1 as the expanded 3-axis comparison with a documented scope caveat rather than forcing the original ranking check.

**Acceptance criteria:**
- **Clean-clone preflight:** from a fresh clone of physics-lint and `dogfood/laplace-uq-bench`, executing `scripts/v1_1_dogfood_preflight.sh` (or equivalent documented command sequence) confirms that all required checkpoints, configs, and the diffphys venv resolve before any inference is attempted. Acceptance fails if any path below resolves to a missing file at preflight time.
- Ensemble fallback decided: either upstream adds a `pde_residual` column for `ensemble_phase2` (sanity axis checkable) or the dogfood harness adopts an explicit per-surrogate axis-skip rule, documented in the design doc update.
- OT-CFM / flow-matching checkpoint trained from `dogfood/laplace-uq-bench/configs/flow_matching.yaml` and stored at `experiments/flow_matching/best.pt` (or Modal volume equivalent; verify volume name at v1.1 start, not from stale plan notes).
- Improved DDPM checkpoint available: `dogfood/laplace-uq-bench/configs/ddpm_improved.yaml` + `experiments/ddpm_improved/best.pt`.
- DPS sampler wired through a `dogfood/_extract_predictions.py` branch that invokes `dogfood/laplace-uq-bench/src/diffphys/model/dps_sampler.py` over the existing unconditional DDPM checkpoint. No new weights required.
- All ≥ 6 surrogates run through the existing 3-axis harness with per-surrogate SARIF category (already supported in V1 Week 4).
- 3-axis verdict produced for the expanded set; if verdict is `PASS (scoped)` or `PASS (scoped, MIXED)` on n=6, Criterion 3 is met in expanded-scoped form. Ranking-check reinstatement is an optional v1.1 stretch goal gated on an H¹-comparable metric becoming available.
- README summary updated: v1.0 3-surrogate result archived; v1.1 expanded result becomes the current lede.

**V1.1 implementation cost estimate:** ~2–3 days. OT-CFM/flow-matching and improved-DDPM training are the dominant costs (~1 day on Modal A100 per model if `configs/*.yaml` still runs cleanly; budget a half-day of debugging per model if they don't). DPS wiring is ~half a day given the existing `dps_sampler.py`. Ensemble sanity-axis reinterpretation is ~half a day of design work plus a few hours of harness update. Not in V1 because the Week 2 compounding issues (no checkpoints in git, no laptop GPU for fallback D) combined with Week 2½'s one-engineering-day timebox narrowed feasible scope to the 3 already-trained-and-published-column surrogates.

**Related V1 design discipline:**
- Week 4 SARIF category support handles per-surrogate isolation already; adding surrogates is purely a data/adapter addition, not a rule-engine or SARIF change
- v1.1 should close Week 1's clean-clone-verification gap (see `docs/tradeoffs.md` 2026-04-16 takeaway) with the preflight script above and a `make dogfood` CI target so that stale path references — of the kind Codex flagged in this backlog's first draft (`configs/cfm.yaml` and `samplers/dps.py`, both nonexistent) — fail loudly at v1.1 start instead of being discovered during execution

---

## 2026-04-17: metrics-compatibility shim — make the A1 sanity axis a byte-identical comparison

**V1 source:** `docs/tradeoffs.md` 2026-04-17 Phase 3 entry; `preflight/2026-04-17_preflight.log` §Investigation; `src/physics_lint/rules/ph_res_001.py`; `dogfood/laplace-uq-bench/src/diffphys/evaluation/metrics.py::pde_residual_norm`.

**V1 state.** Phase 3 executed on 2026-04-17 produced a mechanical `BUG` verdict because physics-lint's PH-RES-001 (fd4 stencil, full 64×64 grid, L² trapezoidal quadrature) and upstream's `pde_residual_norm` (fd2 5-point stencil, interior 62×62 only, dimensionless RMS) compute *definitionally different* quantities. The design doc §6.5 disclosed this ("not a 1:1 reimplementation") but the 4-way verdict rule still fires `BUG` mechanically on a sanity-axis rank mismatch. At the Phase 3 scope (n=100, n_s=1) the UNet/FNO rank-2/3 swap was 3% in physics-lint vs 19% upstream — plausibly inside sampling variance, but impossible to prove without a re-run at upstream scope (15 h DDPM CPU, blows the 6 h timer). The net result: `BUG` verdict, no discretization bug identified, Criterion 3 deferred to v1.1.

**Root cause.** The sanity axis is supposed to be "structurally degenerate" — a direct comparability check between physics-lint and upstream. Today it isn't degenerate because the two implementations deliberately differ on three independent axes (stencil order, scope, norm). Making the sanity axis actually degenerate requires one side to emit the other's exact quantity, byte-identically.

**V1.1 target.** Add a `metrics_compatibility` mode to PH-RES-001 (and as a pattern, to the other rules that have upstream cousins) that emits an auxiliary `upstream_compatible_raw_value` field alongside the default `raw_value`. On Laplace/Dirichlet non-periodic problems, the compatibility mode:
- Uses the classical 5-point fd2 Laplacian stencil (not fd4), with coefficients exactly matching `diffphys/evaluation/metrics.py:23-27`.
- Restricts the residual to the interior `(H-2) × (W-2)` subgrid, excluding the boundary ring.
- Takes the dimensionless RMS `√(mean(r²))` rather than the L² trapezoidal `√(∫|r|² dA)` (drops the `h²` factor).

Expose this via a new rule kwarg (`upstream_compatibility_mode: bool = False`) or a separate comparator helper that the A1 harness can call without polluting PH-RES-001's own default semantics. The A1 harness's sanity axis then compares `upstream_compatible_raw_value` against upstream's `pde_residual_norm`; the verdict rule fires `BUG` only when the two byte-identical-quantity values genuinely rank differently — at which point a sampling-variance-at-reduced-n explanation is no longer available and the ranking mismatch is a real discretization bug to fix.

**Acceptance criteria:**
- `metrics_compatibility` helper or mode exists on PH-RES-001 (Laplace / Dirichlet / non-periodic path only in v1.1; Poisson / heat / wave compatibility is out of scope).
- On the three Phase 3 checkpoints, the compatibility-mode output matches `diffphys/evaluation/metrics.py::pde_residual_norm(field, h=1/63)` to within `1e-6` (fd2 vs fd2 should be bitwise identical modulo float32 reduction order; allow a tiny tolerance for associativity).
- The A1 harness (`dogfood/run_dogfood_real.py`) switches the sanity axis to the compatibility-mode output. The 4-way verdict rule stays the same.
- Regression test: a unit test checks that on `f(x,y) = x*(1-x)*y*(1-y)` (a Poisson solution, not Laplace, but a clean analytic reference), PH-RES-001 default value and compatibility-mode value differ in the expected direction (default larger by the scope factor), and neither is NaN or dominated by boundary stencil noise.
- Re-run Phase 3 under v1.1 scope (n ≥ 300, n_s ≥ 5 for DDPM) against the 6-surrogate set with the compatibility-mode sanity axis. If verdict is still `BUG`, the remaining gap is a real discretization bug to investigate separately.
- README updated to document that physics-lint ships two residual norms per rule (variationally-correct default, upstream-compatible comparison) and when to use each.

**V1.1 implementation cost estimate:** ~0.5–1 day. The fd2 + interior-only + RMS path is 20 lines of numpy; the rule API extension for an auxiliary value is another half-day including tests. The re-run is compute cost, covered by the 6-surrogate dogfood entry above.

**Related V1 design discipline:**
- The default PH-RES-001 value (fd4, full-grid, L²) is the variationally-correct choice per the Bachmayr / Ernst citations and stays the primary rule output; compatibility mode is an *additional* value for direct upstream comparison, not a replacement.
- The same pattern applies to PH-POS-002 (today's Phase 3 showed a binary-axis mismatch between physics-lint's overshoot-magnitude threshold and upstream's violation-count threshold; a `max_viol_count_compatible_value` emission would resolve that axis too).
- This entry is specifically a v1.1 backlog item, not a v1.0 blocker — the design-doc §6.5 disclaimer correctly anticipated the issue, but the plan's verdict-rule wiring did not soften the `BUG` branch when the scope of disagreement was definitional rather than numerical. Future plans that define a cross-tool comparison should either (a) make both sides emit the comparee's exact quantity, or (b) soften the verdict so a definitional gap reads as `INCOMPARABLE` rather than `BUG`.

---

## 2026-04-16: marketing scatter plot — "MSE misses what physics catches"

**V1 source:** `docs/tradeoffs.md` 2026-04-16 Virkkunen entry; Week 2 Task 10.

**V1 state.** Deferred. `dogfood/run_regime_comparison.py` ships a regime-comparison table on synthetic defects, which exercises the rule pipeline across BC regimes but is not the advertised figure. The Week 2½ real-ML run produces a 4-point in-distribution scatter of MSE vs H⁻¹ residual as a byproduct; this ships in the V1 README as a scoped figure but does not carry the "MSE misses what physics catches" claim because it lacks OOD cluster separation.

**V1.1 target.** Produce the full scatter plot: x-axis MSE (or validation L² error), y-axis physics-lint aggregate H⁻¹ residual, one point per surrogate per test dataset, with an in-distribution cluster and an OOD cluster that visibly separate under physics-lint but not under MSE. Requires (a) all 6 surrogates from the OT-CFM + DPS restoration entry above, (b) a distribution-shift test set with clearly different inductive-bias behaviour across models.

**Acceptance criteria:**
- All 6 laplace-uq-bench surrogates evaluated (inherits the adapter-restoration acceptance criteria above)
- A distribution-shift test set available: either a validated Virkkunen-class dataset if one is identified, or a constructed OOD set using held-out BC regimes / forcing terms from the laplace-uq-bench problem family. The constructed option is acceptable if the construction is documented and the shift produces visible model disagreement.
- Scatter plot shows ≥1 model pair where MSE ranking and physics-lint ranking invert, corroborating the "MSE misses what physics catches" claim
- Figure is committed to `docs/figures/` with a standalone caption explaining the axes, the test set, and the interpretation; referenced from the README

**V1.1 implementation cost estimate:** ~0.5–1 day once the 6-surrogate dogfood is restored. Dominant cost is constructing or sourcing the OOD test set, not the plotting itself.

**Related V1 design discipline:**
- The V1 regime-comparison table on synthetic defects is a real artifact and stays in the repo — it validates that rule scoring varies across BC regimes, which is a useful self-test. v1.1 adds the real-ML scatter on top; it does not replace the synthetic-defect self-test.

---

## 2026-04-17: CLI `check` — auto-extract per-rule kwargs instead of skipping

**V1 source:** Week 4 Task 2 `src/physics_lint/cli/check.py` (signature-based skip + SKIPPED RuleResult emission); Codex review on feature/v1-week-4 Tasks 0-3 (Priority 1 sub-question (c)); Week 4 plan §Task 2 Step 3 "Note on the TypeError branch" (documented V1 limitation); `docs/tradeoffs.md` 2026-04-17 Phase 3 and Codex-review entries (silent-correctness-failure pattern).

**V1 state.** The CLI iterates the rule registry and invokes each rule as `check_fn(loaded.field, loaded.spec)`. Three rules require additional kwargs: `PH-BC-001` needs `boundary_target`, `PH-POS-002` needs `boundary_values`, `PH-NUM-002` needs `refined_field`. The Week 4 CLI detects these via `inspect.signature` before invocation (so rule-internal `TypeError` propagates instead of being swallowed — see Codex Priority 1 (a)) and emits a `SKIPPED` `RuleResult` with `reason="requires <kwargs> (CLI V1 limitation; V1.1 auto-extracts)"`. The skip is visible in text summary (⊘ glyph), JSON output, and SARIF `toolExecutionNotifications` (never in `run.results`, so the Security tab stays clean). From the CLI, PH-BC-001 / PH-POS-002 / PH-NUM-002 do not run in V1.0 — they skip cleanly rather than fire half-configured.

**Why it's a V1 limitation, not a V1 bug.** Two of the three kwargs (`boundary_target`, `boundary_values`) can be derived from the loaded target automatically: `boundary_target` for a Dirichlet BC can default to `field.values_on_boundary()` (the rule then measures self-consistency, which is degenerate but not wrong); `boundary_values` for PH-POS-002 is the same data by a different name. `refined_field` for PH-NUM-002 requires a *second* grid at 2× resolution, which either has to be loaded separately (new loader contract) or computed via re-running a user-supplied model at finer resolution (adapter contract change). The first two are mechanical; the third is a design decision the V1 CLI scope did not budget for.

**V1.1 target.** Extend the CLI dispatch loop to auto-populate `boundary_target` and `boundary_values` from `loaded.field.values_on_boundary()` when the BC is Dirichlet / Dirichlet-homogeneous and the rule's signature asks for them. For PH-NUM-002, either (a) accept a `--refined-target <path>` CLI flag that loads a second dump at finer resolution, (b) extend adapter contract to emit a refined prediction alongside the primary, or (c) skip the rule cleanly with a clearer reason (documented trade-off between "degenerate but informative self-check" and "genuine convergence rate"). Choose (a) or (b) after a cost pass.

**Acceptance criteria:**
- `physics-lint check <target>` invokes PH-BC-001 and PH-POS-002 automatically when the loaded target has a Dirichlet BC; the rules run and produce PASS/WARN/FAIL (not SKIPPED) on a representative laplace-uq-bench dump.
- `--refined-target <path>` CLI flag OR adapter `domain_spec()` emitting a second prediction enables PH-NUM-002 in the automatic dispatch; the rule runs on at least one real surrogate.
- The current signature-based-skip path remains as a fallback for rules whose requirements can't be auto-derived, with the same SKIPPED RuleResult emission the V1 CLI uses.
- Tests: for each auto-extracted rule, a CLI-level test verifying it runs (not SKIPPED) on a dump whose spec satisfies the extraction preconditions.
- No regression on the Codex Priority 1 (a) contract: rule-internal `TypeError` still propagates; the signature-based precheck is the only skip path.

**V1.1 implementation cost estimate:** ~1 day. BC-related extraction is ~1 hour + tests. PH-NUM-002 refined-target design + implementation is ~4–6 hours depending on which option is chosen. Test coverage for auto-extraction path is ~2 hours.

**Related V1 design discipline:**
- The V1 signature-based-skip pattern is not a hack, it is the correct structural fix for the silent-correctness-failure pattern caught in the Codex review (Priority 1 (a)). V1.1 auto-extraction is an *extension* of that pattern — "if we know how to derive the kwarg from the loaded target, we do; otherwise we fall back to SKIPPED." The skip path is retained as the floor, not removed.
- SKIPPED RuleResult emission (Codex Priority 1 (b)) is permanent, not a V1 workaround. Any skipped rule — signature-skipped, spec-mismatched, or explicitly disabled by `--disable` — must always surface in the user-visible summary so users can see which rules did and did not fire.
- **V1.0 partial auto-extraction shipped.** The CLI already auto-extracts `boundary_target` and `boundary_values` for (a) dumps that ship a `boundary_target` array in their .npz (`dogfood/make_ci_dumps.py` does this for Week 2½ surrogates) and (b) `dirichlet_homogeneous` BCs (zeros on the boundary). V1.1 extends to non-homogeneous dirichlet without a shipped array, and to `PH-NUM-002` `refined_field`.

---

## 2026-04-17: CLI per-rule override surface (`[tool.physics-lint.rules]`)

**V1 source:** Codex review on feature/v1-week-4 Tasks 4-6 (Finding 4); `docs/design/2026-04-14-physics-lint-v1.md` §12 CLI + config; Week 4 plan Task 3 `config show` / `config init` subcommands.

**V1 state.** The design doc specifies per-rule override syntax in `[tool.physics-lint.rules]`:

    [tool.physics-lint.rules]
    "PH-RES-001" = { tol_pass = 10.0, tol_fail = 100.0 }
    "PH-BC-001"  = { abs_threshold = 1e-10, abs_tol_fail = 1e-3 }
    "PH-SYM-003" = { enabled = false }
    "PH-CON-003" = { severity = "error" }

`DomainSpec` parses this block (pydantic v2 accepts the nested dict), but the V1.0 CLI does not apply any of the four override kinds — the block is read and ignored. The V1.0 workaround is `--disable <RULE_ID>` at the command line for the `enabled = false` case; `tol_pass`, `abs_threshold`, and `severity` have no V1.0 equivalent.

**V1.1 target.** Wire each override kind through the CLI's rule dispatch:

- `tol_pass` / `tol_fail` / rule-specific floats (`abs_threshold`, `abs_tol_fail`): pass through as kwargs to the rule's `check()` function. Each rule already accepts its thresholds as keyword-only parameters with defaults, so the plumbing is a dict lookup + kwargs merge at dispatch.
- `enabled = false`: treat the rule as if it were in `--disable`.
- `severity = "<level>"`: override `entry.default_severity` on the RegistryEntry before rule invocation, and thread the override through `RuleResult.severity` so SARIF level mapping and `exit_code` computation use the override.

**Acceptance criteria:**

- CLI-level tests for each override kind on at least one real rule (e.g. `PH-RES-001 tol_pass`, `PH-SYM-003 enabled = false`, `PH-CON-003 severity = error`).
- `physics-lint config show` surfaces which overrides were loaded from TOML (already partially done — it prints the full resolved spec — but should add a "overrides active on this run" summary for readability).
- The README `[tool.physics-lint.rules]` documentation can return as a live (not "design-doc future") feature.
- No regression on `--disable RULE_ID`; the CLI flag and the TOML `enabled = false` can coexist (CLI flag wins per override precedence in `merge_into_spec`).

**V1.1 implementation cost estimate:** ~0.5 day. The dispatch plumbing is ~50 LOC; the hardest part is threading `severity` overrides through the report assembly without breaking `exit_code` semantics. Test coverage is another ~0.5 day.

**Related V1 design discipline:**
- The V1.0 README was corrected to mark the `[tool.physics-lint.rules]` block as design-doc future surface, not live v1.0 feature. The same principle applies to any documented config: if the CLI doesn't apply it, the README should not claim it. See the feedback_codex_review memory entry for the pattern.

---

## 2026-04-17: pre-commit hook multi-target + `--rule` self-test

**V1 source:** Codex review on feature/v1-week-4 Tasks 4-6 (Findings 3 and 5); `.pre-commit-hooks.yaml` V1.0 stub; `src/physics_lint/cli/self_test.py` V1.0 signature.

**V1 state.** Two small CLI UX limitations.

(1) **Pre-commit hook accepts only one target.** The shipped `.pre-commit-hooks.yaml` uses `pass_filenames: false` because `physics-lint check` accepts exactly one target argument and pre-commit by default passes every matched file as an argument — which produces a Typer "unexpected extra argument" error. Downstream users must add `args: [path/to/adapter_or_dump]` in their `.pre-commit-config.yaml` to specify the target. This is a viable pattern (users explicitly choose which adapter/dump the hook lints) but not the "lint all changed files" ergonomic most pre-commit users expect.

(2) **`self-test --rule <id>` not supported.** The V1.0 CLI signature dropped the `--rule` option (previously accepted but ignored) to avoid the misleading-API pattern. The self-test battery runs all rules on all analytical cases or nothing.

**V1.1 target.**

For (1): either extend `physics-lint check` to accept multiple targets and loop internally, OR introduce a new subcommand `physics-lint check-batch <paths...>` used only by the pre-commit hook. The batch path emits one RuleResult set per target and aggregates into a single report (useful for pre-commit output).

For (2): wire `--rule <PH-XXX-NNN>` through `physics_lint.selftest.run()` — filter the analytical cases to those that exercise the named rule and run only those. Useful for fast iteration while debugging a single rule.

**Acceptance criteria:**

- `physics-lint check-batch a.npz b.py c.npz` produces a single aggregate report covering all three targets; pre-commit hook updated to `pass_filenames: true` + `entry: physics-lint check-batch`.
- `physics-lint self-test --rule PH-RES-001` runs only the analytical cases that exercise PH-RES-001 and exits 0 iff those cases pass.
- Existing single-target `physics-lint check` and all-rules `physics-lint self-test` behavior unchanged.

**V1.1 implementation cost estimate:** ~0.5 day each.

---

## 2026-04-17: PH-BC-001 / PH-RES-001 relative-mode denominator regularization on homogeneous-Dirichlet samples

**V1 source:** Week 4 Task 4 Step 0 interpretation pass (2026-04-17); README §"v1.0 known limitations"; per-model verdict table in commit `955ebf1` message. Artifacts: `dogfood/laplace_uq_bench/{unet_regressor,fno,ddpm}_pred.npz` (sample_index=0, homogeneous Dirichlet).

**V1 state.** `PH-BC-001` in relative mode divides the raw boundary error by `avg|boundary_target|`, with a floor at machine epsilon (~2.2e-16) when the denominator would underflow. On homogeneous-Dirichlet samples (boundary target ≡ 0), the floor dominates, producing `ratio` values of ~1e13–1e14 that trip the FAIL threshold for *any* non-zero raw error. Observed on all three Week 2½ surrogates: UNet raw=1.56e-02 → ratio=7.04e13; FNO raw=1.92e-01 → ratio=8.64e14; DDPM raw=1.04e-02 → ratio=4.70e13. `PH-RES-001` has the same pattern with a 1e-12 floor: UNet raw=45.0 → ratio=4.48e13; FNO raw=51.7 → ratio=5.15e13; DDPM raw=25.6 → ratio=2.55e13.

**Symptoms.** The v1.0 CI workflow on `sample_index=0` of `tyy0811/laplace-uq-bench` shows all three models FAIL `PH-BC-001` and `PH-RES-001`. Raw-value *ordering* is still correct (FNO > UNet > DDPM on both rules, matching upstream `bc_err` and `pde_residual` rankings) and so the Week 2½ 3-axis ranking agreement is unaffected. But absolute `PASS`/`FAIL` verdicts on such samples are threshold artifacts, not physics signals. This is disclosed in the v1.0 README limitations section as a docs entry; the underlying code fix is this v1.1 backlog item.

**V1.1 target.** Regularize the relative-mode denominator with `max(avg|ref|, absolute_floor)` where `absolute_floor` is a calibrated per-problem-class value (e.g. 1e-3 for BC errors on unit-domain Laplace), not machine epsilon. The floor should be loaded from `src/physics_lint/data/floors.toml` alongside the existing rule floors. Consider either:

- **Option A:** add a new `relative_mode_floor` key per rule in `floors.toml`, distinct from the existing `denom_epsilon`. Clearer semantics.
- **Option B:** promote the existing `denom_epsilon` from machine epsilon to a calibrated value. Simpler but loses the distinction between "underflow guard" and "physically-meaningful floor."

Recommend Option A.

**Acceptance criteria:**

- New `floors.toml` key (e.g. `relative_mode_floor`) for `PH-BC-001` and `PH-RES-001`, calibrated via the existing `scripts/calibrate_floors.py` on a representative laplace-uq-bench sample mix.
- On the v1.0 CI dumps (`dogfood/laplace_uq_bench/{unet_regressor,fno,ddpm}_pred.npz`), `PH-BC-001` relative-mode ratios drop from ~1e13–1e14 to ~1e0–1e2 (meaningful magnitudes). Raw-value ordering unchanged.
- On the same dumps, UNet and DDPM `PH-BC-001` move from FAIL → PASS (raw values 0.016 and 0.010 are below threshold when the denominator is a physical floor, not machine epsilon). FNO stays FAIL (raw 0.192 is above threshold). Matches the dogfood ranking narrative.
- `PH-RES-001` likewise: UNet/DDPM move from FAIL → PASS or WARN on `sample_index=0`; FNO stays FAIL.
- New test case `tests/rules/test_ph_bc_001_homogeneous_dirichlet.py` with a synthetic homogeneous-Dirichlet dump that exercises the floor path explicitly and asserts sensible ratio magnitudes.
- v1.0 README limitations section updated to note the v1.1 fix shipped (demoted to historical context, not active limitation).
- CI workflow comments referencing the "all 3 fail" symptom on `sample_index=0` updated.

**V1.1 implementation cost estimate:** ~1 day. Floor calibration is ~2 hours on existing laplace-uq-bench dumps. Rule code changes are ~20 LOC total across `ph_bc_001.py` and `ph_res_001.py`. Test case is ~1 hour. README limitations demotion + CI comment cleanup is ~30 min. Regression check across the full test suite and 3-surrogate dogfood is another ~2 hours.

**Related V1 design discipline:**

- The V1.0 hero pivoted from `PH-BC-001` to `PH-POS-002` during the 2026-04-17 interpretation pass precisely because `PH-BC-001`'s relative-mode FAIL verdict was unreliable on the sample where the screenshot was to be captured. `PH-POS-002` is an absolute-threshold rule and doesn't suffer this issue. V1.1's fix restores `PH-BC-001` to hero-eligible status; the v1.1 README may optionally re-pivot the hero back, depending on which screenshot reads best.
- The floor-calibration pattern in `src/physics_lint/data/floors.toml` was introduced in Week 1/2 for different reasons (avoiding false-positive FAILs on numerically-converged predictions). Extending the pattern to cover the homogeneous-Dirichlet denominator case is a principled use of the same mechanism, not a new hack.
- V1 `docs/tradeoffs.md` should gain a 2026-04-17 entry citing this backlog item as the resolution path for the all-3-FAIL symptom on CI sample 0 — the tradeoff was "ship v1.0 with documented limitation vs hold for fix"; documented limitation won because the rank-ordering is still correct and the hero pivot preserved the marketing narrative.

---

## 2026-04-20: external validation — PH-RES-002 (Tier B Task 6)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 6.

**Deferral rationale (spec §9 item 4):** "Defer Tier-B plan commitment until Tier A is green and visa-deadline picture is clearer (post-~May 5 when Week 4 + Tier A complete). Tier B budget: re-estimate from empirical Tier-A velocity, not from this plan's a priori estimates."

**V1.1 target.** FD-vs-AD residual cross-check anchored to Chiu et al., "CAN-PINN," *CMAME* 395:114909 (2022), arXiv:2110.15832 (AD vs 2nd-order ND discrepancy framework).

**Acceptance criteria (spec §4 Task 6 Test design).** MMS smooth solution; compute residual via `torch.autograd.functional.jacobian` and via 4th-order Fornberg FD; assert discrepancy is O(h⁴) on a sequence of refinements.

**Risks.** Non-smooth activations (ReLU) produce distributional AD derivatives that differ sharply from FD even on smooth inputs; gate the rule on smooth-activation models per PH-NUM-003.

**Effort estimate.** ~1 engineer-day.

**Post-Tier-A finding (2026-04-21, Category 2).** Tier-A Task 4 (PH-RES-001 Layer 1) surfaced an interior-vs-boundary stencil split in the Fornberg FD4 Laplacian at `src/physics_lint/field/grid.py:22-31`: interior [2:-2, 2:-2] is O(h⁴), outer two layers are O(h²) via one-sided/off-center stencils. The full-domain L² residual converges ~O(h^{3.5}), not O(h⁴). PH-RES-002's acceptance criterion "discrepancy is O(h⁴)" uses the same FD stencil family. Before writing final v1.1 acceptance criteria, apply the enumerate-the-splits procedure (see `feedback_deviation_pattern_escalation.md`): determine whether the AD-vs-FD discrepancy on the full domain converges at the interior O(h⁴) rate or inherits the boundary O(h²) component, and choose the reproduction domain accordingly. The Tier-A Path A′ pattern (clean-regime reproduction + fallback-regime characterization) is a known-working shape for this.

---

## 2026-04-20: external validation — PH-RES-003 (Tier B Task 7)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 7.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Spectral-vs-FD residual cross-check on periodic grids, anchored to Trefethen *Spectral Methods in MATLAB* (SIAM 2000), Chapter 3 (spectral accuracy). *Chapter and theorem number to be verified against local copy at execution time.*

**Acceptance criteria (spec §4 Task 7 Test design).** Periodic fixture `u = cos(2πx)cos(2πy)` on torus; compute residual via `torch.fft` spectral derivative and via 4th-order Fornberg FD; discrepancy should be O(h⁴) — FD converges algebraically, spectral converges exponentially, so their difference follows the slower rate.

**Risks.** On under-resolved data (energy above Nyquist), both estimators are wrong; rule should warn if input spectrum has >1% energy above `f_s / 4`.

**Effort estimate.** ~0.5 engineer-day.

**Post-Tier-A finding (2026-04-21, Category 2).** PH-RES-003 uses the same Fornberg FD4 stencil family as PH-RES-001/002; the interior-vs-boundary O(h²) boundary-stencil split (see PH-RES-002 entry above and `grid.py:22-31`) applies here too. *However*, the spec specifies a periodic fixture on the torus, which eliminates the boundary-stencil regime entirely (periodic = no boundary = interior stencil everywhere). The Tier-A discovery is therefore a constraint-check rather than a reconfiguration trigger: verify at v1.1 start that the fixture remains strictly periodic and `np.roll`-based differentiation applies uniformly. Task 4 Layer 2a's `periodic+spectral` calibration in `external_validation/PH-RES-001/fixtures/norm_equivalence_bounds.json` is a reference configuration.

---

## 2026-04-20: external validation — PH-BC-001 (Tier B Task 8)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 8.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Boundary condition violation rule anchored to PDEBench (Takamoto et al., *NeurIPS 2022*, arXiv:2210.07182) bRMSE metric definition (Appendix B of the supplement); Sukumar & Srivastava, *CMAME* 389:114333 (2022), arXiv:2104.08426 for tolerance context (qualitative only — specific exponent bands not pinned in the cleaned-draft spec).

**Acceptance criteria (spec §4 Task 8 Test design).** Dirichlet, Neumann, periodic fixtures on unit square with known BC traces; run PH-BC-001 in both relative and absolute modes; assert correct verdict on each.

**Risks.** Relative mode on homogeneous Dirichlet is the denominator-blow-up issue flagged in v1.0 limitations and in the 2026-04-17 `PH-BC-001 / PH-RES-001 relative-mode denominator regularization` backlog entry above; tests must cover this failure mode explicitly.

**Effort estimate.** ~1 engineer-day.

**Cross-reference.** The pre-existing 2026-04-17 backlog entry covers the relative-mode denominator fix. This Tier-B entry covers the *full* PH-BC-001 external-validation anchor (all three BC families, both measurement modes, known-verdict fixtures). The two entries are complementary: the 2026-04-17 entry is a rule-primitive fix; this entry is an external-validation reproduction that the rule behaves correctly on canonical BC fixtures. Execute the 2026-04-17 fix before this entry's anchor so the homogeneous-Dirichlet case doesn't produce threshold-artifact FAILs.

---

## 2026-04-20: external validation — PH-BC-002 (Tier B Task 9)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 9.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Boundary flux imbalance rule anchored to the classical Gauss-Green (divergence) theorem. MMS reference: analytic `F = (x²y, xy²)`, `∇·F = 2xy + 2xy = 4xy`, `∫_[0,1]² 4xy dx dy = 1`.

**Acceptance criteria (spec §4 Task 9 Test design).** Compute LHS and RHS of divergence theorem on analytic F at four grid resolutions; assert imbalance → 0 at observed O(h²) or O(h⁴) depending on stencil.

**Risks.** Surrogate outputs scalar u, not F; rule must construct F from u. For FNO on periodic torus, `∫_∂Ω F·n = 0` trivially — test requires non-periodic domain.

**Effort estimate.** ~1 engineer-day.

**Post-Tier-A finding (2026-04-21, Category 2).** The spec's acceptance "observed O(h²) or O(h⁴) depending on stencil" is already enumerate-the-splits-aware (the "depending on stencil" clause anticipates the regime distinction). Tier-A Task 4 Layer 1 confirmed the empirical structure: interior Fornberg FD4 hits 3.99, full-domain hits 3.49 (not 4); the boundary-stencil O(h²) component contaminates the full-domain rate. For PH-BC-002 specifically, the divergence-theorem LHS (surface integral over ∂Ω) necessarily samples boundary stencils by construction, unlike PH-RES-002 where the interior reproduction is available. v1.1 acceptance should pin the LHS-vs-RHS imbalance rate empirically against the surface-integral quadrature chosen, not against the interior O(h⁴) ideal. Apply enumerate-the-splits procedure at planning time.

---

## 2026-04-20: external validation — PH-SYM-003 (Tier B Task 10)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 10.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** SO(2) Lie derivative equivariance (Gruver LEE), with a three-layer anchor structure explicitly named in spec §4 Task 10:

- **Layer 1 — metric definition (paper anchor).** Gruver, Finzi, Goldblum, Wilson, "The Lie Derivative for Measuring Learned Equivariance," *ICLR 2023*, arXiv:2210.02984. Equation (4) defines LEE; Figure 3 sketches the PyTorch implementation via `torch.autograd.functional.jvp` on a grid-sample-rotated model.
- **Layer 2 — implementation faithfulness (reference-code anchor).** github.com/ngruver/lie-deriv (MIT). Runs the repo's `exps_e2e.py` pipeline on ResNet-50 + ImageNet and compares output to the repo's own pipeline.
- **Layer 3 — stability-regression target (local).** A pinned LEE value from the author's own first measurement on a specific fixture; ±20% tolerance for future runs. *Not* a paper-reproduction claim.

**Acceptance criteria (spec §4 Task 10).**
- LEE metric implementation passes unit tests against Gruver Figure 3 reference sketch (Layer 1).
- Reproduction script produces a translation-LEE value within ±5% of `ngruver/lie-deriv` `exps_e2e.py` output on same inputs (Layer 2 — tight, same-code comparison).
- Pinned Layer-3 regression target documented in CITATION.md with fixture description (sample count, preprocessing, ResNet-50 weight SHA, hardware class, date); future runs reproduce within ±20%.
- CI skip-on-missing-`$IMAGENET_VAL_DIR` produces clear message linking reproduction instructions.

**Risks.** Layer 1 domain mismatch (Gruver evaluates ImageNet classifiers, physics-lint targets PDE surrogates — metric transfers, absolute thresholds don't). Layer 2 reference-code drift (pin `ngruver/lie-deriv` commit SHA). Layer 3 single-measurement uncertainty (±20% is conservative; tighten after multiple measurements). Reflection-padding vs circular-padding design difference is intentional.

**Effort estimate.** ~2 engineer-days (0.5 LEE metric + unit tests; 0.5 reproduction script + env-var gate + same-code comparison; 0.5 Layer-3 pin + fixture documentation; 0.5 README + CITATION).

**Harness dependency.** Shares the symmetry-family anchor lineage with PH-SYM-001/002 (Tier-A Task 3) and PH-SYM-004 (Tier-B Task 11). The Tier-A symmetry harness at `bdc0a0b (feat(external-validation): symmetry harness - FFT Laplace inverse + rotate/reflect tests)` + `3ba694d (feat(external-validation): PH-SYM-001 + PH-SYM-002 rule anchors)` ships the FFT Laplace-inverse primitive and rotate/reflect fixtures. PH-SYM-003's Gruver LEE metric is *not* a direct reuse of the Tier-A harness (it depends on `torch.autograd.functional.jvp` on ResNet-50 + ImageNet, which the Tier-A harness doesn't cover), but the symmetry-harness discipline — positive and negative controls, reference-code comparison where available — applies as the structural template.

---

## 2026-04-20: external validation — PH-SYM-004 (Tier B Task 11)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 11.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Translation equivariance on periodic grids, anchored to Cohen-Welling 2016 (G-CNN framework) + Helwig et al. 2023 §2.2 (FNO translation-equivariant by design on periodic grids).

**Acceptance criteria (spec §4 Task 11 Test design).** Inherits the symmetry harness from Tier-A Task 3. Random integer shifts via `torch.roll` on periodic fixtures; FFT-based Laplace inverse as positive control (exactly translation-equivariant; **reuses Task 3's zero-mode convention** — `û(k=0) = 0` on the inverse); non-equivariant CNN with positional embeddings as negative control.

**Risks.** Integer shifts hide subpixel aliasing; PH-SYM-003 (LEE) catches that. Document the complementarity between PH-SYM-004 (integer shifts) and PH-SYM-003 (continuous SO(2)).

**Effort estimate.** ~0.25 engineer-day (harness-reuse from Tier-A Task 3).

**Harness dependency.** The symmetry harness landed as Tier-A Task 3 at commits `bdc0a0b (feat(external-validation): symmetry harness - FFT Laplace inverse + rotate/reflect tests)` and `3ba694d (feat(external-validation): PH-SYM-001 + PH-SYM-002 rule anchors)` — 4 harness tests + 8 anchor tests. The FFT Laplace-inverse primitive (positive control) and the `torch.roll` translation fixture ship with PH-SYM-001/002. PH-SYM-004 is a thin additional rule on top of the same harness.

---

## 2026-04-20: external validation — PH-CON-001 (Tier B Task 12)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 12.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Mass conservation (heat) rule anchored to Hansen, Maddix, Alizadeh, Gupta, Mahoney, "Learning Physical Models that Can Respect Conservation Laws," *Physica D: Nonlinear Phenomena* 457 (2024), 133952, DOI 10.1016/j.physd.2023.133952 (peer-reviewed primary citation; earlier conference: *ICML 2023*, PMLR 202:12469–12510; preprint arXiv:2302.11002). Table 1 diffusion CE numbers at `t = 0.5`. Code: github.com/amazon-science/probconserv.

**Acceptance criteria (spec §4 Task 12 Test design).**
1. Port ProbConserv's 1D CE definition to 2D heat: `CE(t) = |∫u(·,t) - ∫u(·,0)| / |∫u(·,0)|`.
2. Analytical fixture: periodic heat on `[0,1]²`, IC `u(x,y,0) = 1 + 0.1 sin(2πx)sin(2πy)`, mass-conserving (periodic BC).
3. Assert `CE(t) < 10⁻⁶` at `t ∈ {0.01, 0.05, 0.1}` on analytical solution.
4. Citation anchor: README cites Hansen Table 1 as "diffusion-family conservation precedent; threshold is physics-lint calibration, not a 1D→2D port of Hansen's value."

**Risks.** Hansen is 1D; physics-lint target is 2D/3D. Cited as precedent, not as threshold source — must be explicit in README. Negative control (inject small mass drift; assert FAIL) required to avoid self-passing.

**Effort estimate.** ~2 engineer-days (0.5 CE metric + 0.5 analytical fixture + 0.5 negative control + 0.5 citation).

**Post-Tier-A finding (2026-04-21, Category 3).** PH-CON-001 computes a time-integrated conservation error, structurally analogous to PH-CON-003's energy-dissipation check. Tier-A Task 2 surfaced a rule-primitive bug: `np.gradient(edge_order=2)` produces endpoint artifacts on strictly-dissipative eigenmodes, fixed by a forward-difference primitive at commit `e691dd3 (fix(ph_con_003): forward-difference dE/dt primitive to remove endpoint artifact)`. If PH-CON-001's implementation uses `np.gradient` on the time axis to compute `dM/dt` or a similar primitive, the same endpoint-artifact bug may surface. Before writing v1.1 acceptance criteria, audit the rule's time-derivative primitive and either (a) adopt the Task 2 forward-difference primitive, (b) use time-integrated mass directly (`|M(t) - M(0)|`) avoiding `d/dt` altogether, or (c) characterize the endpoint artifact explicitly if it's unavoidable. This is an *implementation-level* concern, not a spec §4 content change — the spec's CE definition uses instantaneous integrated mass, not `dM/dt`, so acceptance may be cleaner than the CON-003 analogy suggests. Verify at planning time.

---

## 2026-04-20: external validation — PH-CON-002 (Tier B Task 13)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 13.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Wave energy conservation rule anchored to Evans §2.4.3 (wave energy identity); PDEBench cRMSE methodology as secondary citation.

**Acceptance criteria (spec §4 Task 13 Test design).**
1. Self-built 2nd-order FD leap-frog reference on 2D periodic wave, CFL = 0.5.
2. Analytical fixture: `u(x,y,t) = cos(2πt/√2) sin(2πx)sin(2πy)`, exact energy-conserving solution on periodic domain.
3. Run rule on analytical fixture over `t ∈ [0, T]` with T = 10 wave-crossing times; assert `|E(t) - E(0)| / E(0) < 1%`.
4. Negative control: predictions with injected energy drift; assert FAIL.

**Risks.** No ML-surrogate paper reports wave-energy-drift numbers; anchor is classical theory + self-built reference. Discrete E(t) depends on discretization choices; test must use consistent discretization between reference and rule. Leap-frog preserves a *modified* energy to O(Δt²); careful that the reference doesn't look like it's drifting.

**Effort estimate.** ~3 engineer-days (1 leap-frog reference + 0.5 energy functional + 0.5 fixture + assertion + 1 negative control + documentation).

**Post-Tier-A finding (2026-04-21, Category 3).** PH-CON-002 is the wave-equation analog of PH-CON-003 (heat energy dissipation, landed as Tier-A Task 2). Both rules compute time-integrated or instantaneous energy functionals and both presuppose the measurement primitive (time derivative, spatial integral) tracks the continuous-math property. Two Tier-A findings bear on v1.1 execution:

1. **Time-derivative primitive.** Task 2 surfaced the `np.gradient(edge_order=2)` endpoint artifact and fixed it with forward-difference (`e691dd3 (fix(ph_con_003): forward-difference dE/dt primitive to remove endpoint artifact)`, `src/physics_lint/rules/ph_con_003.py`). If PH-CON-002's `dE/dt` primitive uses `np.gradient`, the same bug applies. Adopt the forward-difference primitive or avoid `d/dt` in favor of `|E(t) - E(0)|`.
2. **Leap-frog modified-energy concern.** The risk note above ("leap-frog preserves a *modified* energy to O(Δt²)") is itself an enumerate-the-splits-worthy item: the reference scheme conserves energy only up to modified-equation terms, which the rule's continuous-math anchor (Evans §2.4.3) does not. The 1% tolerance in spec §4 is wider than the modified-energy drift at CFL=0.5, so acceptance is robust — but the v1.1 CITATION.md should name this distinction honestly (Tier-A Path A′ pattern: reproduction on the clean regime, characterization on the fallback regime).

Apply enumerate-the-splits before writing v1.1 acceptance criteria. The self-built leap-frog reference is load-bearing; specify its discretization before building the rule on top.

---

## 2026-04-20: external validation — PH-CON-004 (Tier B Task 14)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 14.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Per-element conservation hotspot (mesh-based) rule anchored to Bangerth-Rannacher *Adaptive FEM for Differential Equations* (Birkhäuser 2003) Ch. 3; Verfürth *A Posteriori Error Estimation* (Oxford 2013) Ch. 1-2. scikit-fem Example 22 (Adaptive Poisson; residual indicators `η_K² = h_K²‖f‖²_{0,K}` and `η_E² = h_E‖[[∇u_h · n]]‖²_{0,E}`) as implementation template.

**Scope.** 2D triangulated meshes only (v1.1); 3D tets deferred to v1.2. Scope boundary is a spec §4 Task 14 decision, not a Tier-A-surfaced amendment — named here at top level so v1.1 execution sees the 2D-only constraint without having to back-reference spec §4.

**Acceptance criteria (spec §4 Task 14, condensed; see spec for full narrative).**
1. Interpolate structured-grid prediction onto `MeshTri` P1 basis; per-element residual via `Functional` weak form.
2. Concentration metric: `hotspot_fraction = #{K : |r_K| / max|r_K| > 0.5} / N_elem`.
3. **Baseline run:** analytical `u = x(1-x)y(1-y)` on uniformly-refined mesh; source `f = -Δu = 2[y(1-y) + x(1-x)]`. Serialize `hotspot_fraction_baseline` to `external_validation/PH-CON-004/fixtures/baseline_calibration.json` with `{n_elem, mesh_hash, calibration_date, scikit_fem_version}`.
4. **Perturbed run:** same `u` + localized bump magnitude 0.01 in disc of radius `2h` near midpoint.
5. **Separation criterion:** `separation = hotspot_fraction_perturbed − hotspot_fraction_baseline > 0.05`; flagged elements of the perturbed run must cluster on the perturbation patch (element-index-level check).
6. **Fixture-mismatch policy:** mesh_hash mismatch → fail hard; scikit_fem minor-patch mismatch → warn; major mismatch → fail; missing fixture → fail with recalibration prompt.

**Risks.** Projection error masquerading as conservation violation (projection error must be << 0.05 separation threshold on baseline). No direct ML-surrogate precedent; anchor is classical FE a posteriori theory. **v1.1 refinement opportunity:** `hotspot_fraction` measures concentration near max only indirectly — `max|r_K| / median|r_K|` (localization/kurtosis) may be more principled. Deferred to v1.1 Path B refinement; v1.0 comparative-separation is a sound floor.

**Effort estimate.** ~3 engineer-days (1 mesh projection + weak-form residual; 1 metric + baseline + separation + localization clustering; 1 documentation + v1.2 3D roadmap).

---

## 2026-04-20: external validation — PH-NUM-001 (Tier B Task 15)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 15.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Quadrature convergence warning (mesh-based), anchored to Ciarlet *FEM for Elliptic Problems* (SIAM Classics 2002) Ch. 4 §4.1 Theorems 4.1.2–4.1.6. *Ciarlet theorem numbers to be verified against local copy at execution time.* scikit-fem Example 1 (Poisson with unit load) as implementation template.

**Acceptance criteria (spec §4 Task 15 Test design).**
1. Smooth integrand `f(x,y) = sin(πx)sin(πy)` on unit square; analytical integral `4/π²`.
2. Sweep `intorder ∈ {1, 2, 3, 4}` × refinement levels 0–5 using `scikit-fem.Basis(..., intorder=k)`.
3. Log-log regression per `intorder`; assert slope `≥ 2·intorder + 2 - 0.5`.
4. Flag saturation at machine precision.

**Risks.** Default `intorder` varies by element; user overrides can trigger spurious warnings. Round-off dominates on very fine meshes.

**Effort estimate.** ~1 engineer-day.

---

## 2026-04-20: external validation — PH-NUM-002 (Tier B Task 16)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 16.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Refinement convergence rate below expected, anchored to Salari-Knupp SAND2000-1444 (Sandia 2000), DOI 10.2172/759450; MASA library (Malaya et al., *Engineering with Computers* 29:487–496, 2013) as code precedent.

**Acceptance criteria (spec §4 Task 16 Test design).**
1. SymPy MMS generator for Laplace/Poisson/heat.
2. Refinement loop: `h/2^k` for `k = 0..4`.
3. L²/H¹ errors; log-log regression of last 3–4 points; assert slope within expected band (`4 ± 0.5` for 4th-order Fornberg Laplace, etc.).
4. Asymptotic-range check: flag if `E_h / E_{h/2}` outside `[2^p · (1 ± 0.25)]`.

**Risks.** Neural surrogates show resolution-invariance rather than classical h^p convergence; rule defaults to WARN (not FAIL) on trained-surrogate inputs per v1.0 design. Rule is primarily for verification, not convergence guarantee.

**Effort estimate.** ~2 engineer-days.

**Harness dependency.** Inherits the refinement harness from Tier-A Task 4 (PH-RES-001). The `_mms_residuals_at` pattern in `external_validation/PH-RES-001/test_anchor.py` is a reference shape for the N-sweep convergence regression.

**Post-Tier-A finding (2026-04-21, Category 2).** PH-NUM-002 *directly* tests convergence rates and therefore sits at the epicenter of Tier-A's interior-vs-boundary stencil discovery. The spec's acceptance "slope within 4 ± 0.5 for 4th-order Fornberg Laplace" holds only for interior error; full-domain L² error on non-periodic domains converges at ~3.5 (measured at `c07ba33 (feat(external-validation): PH-RES-001 Layer 1 Fornberg + full-domain characterization)`, PH-RES-001 Layer 1b). Two paths at v1.1 planning:

- **Path A′** (Tier-A-proven pattern): split the rule into an interior-only convergence check (asserting the textbook 4 ± 0.5) and a full-domain characterization (accepting the empirical boundary-contaminated rate as a positive assertion, not a softening).
- **Path B** (rescope): restrict the v1.1 rule to periodic fixtures where the boundary-stencil regime is eliminated; defer non-periodic convergence to v1.2.

Either path is principled; the current spec wording (single `slope in 4 ± 0.5` band) would fail on the non-periodic MMS fixture shipped with Tier-A. **Tier-A Task 4 Layer 1 resolved the equivalent interior-vs-boundary slope question by Path L1-A** (interior slice for rate claim, full-domain characterization for honest rule behavior); Task 16's v1.1 execution can inherit that resolution pattern directly if the rule's stencil behavior matches Task 4's, compressing the v1.1 decision from "re-derive from first principles" to "inherit the Task 4 precedent or justify deviation." **Apply enumerate-the-splits procedure before finalizing v1.1 acceptance.** See `feedback_deviation_pattern_escalation.md` for the full discipline.

---

## 2026-04-20: external validation — PH-VAR-002 (Tier B Task 17)

**V1 source:** `docs/superpowers/specs/2026-04-20-external-validation-design.md` §4 Task 17.

**Deferral rationale (spec §9 item 4):** same as Task 6 above.

**V1.1 target.** Hyperbolic norm-equivalence conjectural info-flag (documentation-only rule). Citations: Gopalakrishnan-Sepúlveda 2019; Ernesti-Wieners, *Comput. Methods Appl. Math.* 19 (2019), 465–481; Henning-Palitta-Simoncini-Urban, *ESAIM: M2AN* 56 (2022), 1173–1198, arXiv:2107.12119.

**Acceptance criteria (spec §4 Task 17 Test design).**
1. Unit test confirming rule emits INFO-severity warning on any wave-equation invocation.
2. Documentation links the above references.

**Risks.** None — this is a citation-and-warning rule, not a numerical check.

**Effort estimate.** ~0.25 engineer-day.

---

## 2026-04-21: v1.1 plan-writing prep — External Validation README table structure at 18-rule scale

**V1 source:** main `README.md` "External Validation" section (six-row one-per-rule table shipped with Tier-A closeout); Tier-A plan Task 5 Step 6.

**V1.1 plan-writing consideration.** Tier-A ships a six-row table with one row per rule, which works at 6-rule scale. When Tier-B anchors land (Tasks 6–17 above), the table grows to 18 rows, and several Tier-B entries bring layered anchor structures that don't compress cleanly onto a single reference cell — notably PH-SYM-003's three-layer Gruver framing (paper anchor + reference-code anchor + local stability-regression target) and PH-CON-001's "Hansen Table 1 as precedent, not threshold source" calibration-with-scope-note framing. At 18-rule scale, evaluate:

- **Path A** (preferred default): keep one-row-per-rule, compress multi-layer references into a single cell via short labels ("Gruver ICLR 2023 + local regression"); layered detail lives in per-rule `CITATION.md`.
- **Path B** (restructure): group rows by rule family (residual / boundary / conservation / positivity / symmetry / numerics / variational) with a sub-header per family; each row within a family has a compact reference cell.

Path A preserves the scannable at-a-glance coverage property; Path B preserves the layered-anchor detail at the table-cell level at the cost of vertical density.

**Cost.** ~30 min of Tier-B plan-writing design; no implementation cost. Surface alongside whichever Tier-B task ships the first layered anchor (Task 10 PH-SYM-003 is the likely first).

**Not a v1.0 decision.** Tier-A's six-row one-per-rule table is correct for the six-rule scale it ships at. This entry is a forward-reference so Tier-B plan-writing front-loads the table-structure question rather than discovering it mid-execution.
